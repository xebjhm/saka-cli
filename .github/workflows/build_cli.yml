name: Build CLI

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install dependencies (using PyPI pyhako)
        run: uv sync --no-sources

      - name: Build with PyInstaller
        run: uv run --no-sources pyinstaller build.spec

      - name: Smoke Test Binary
        shell: bash
        run: |
          if [ "${{ matrix.os }}" == "windows-latest" ]; then
            ./dist/pyhako-cli.exe --help
          else
            chmod +x ./dist/pyhako-cli
            ./dist/pyhako-cli --help
          fi

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: pyhako-cli-${{ matrix.os }}
          path: dist/pyhako-cli*

      - name: Publish Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: dist/pyhako-cli*
